<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <View
            android:id="@+id/backView"
            android:layout_width="0dp"
            android:layout_height="120dp"
            android:background="@color/color_009788"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent" />

        <ImageView
            android:id="@+id/ivTakePhoto"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:contentDescription="@string/take_picture"
            android:src="@drawable/shape_cicle_radiu_009788_solid"
            app:layout_constraintBottom_toBottomOf="@id/backView"
            app:layout_constraintEnd_toEndOf="@id/backView"
            app:layout_constraintStart_toStartOf="@id/backView"
            app:layout_constraintTop_toTopOf="@id/backView" />

        <ImageView
            android:id="@+id/ivUpload"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@drawable/shape_cicle_radiu_009788_solid_upload"
            android:visibility="gone"
            app:layout_constraintBottom_toBottomOf="@id/ivTakePhoto"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="@id/ivTakePhoto"
            app:layout_constraintTop_toTopOf="@id/ivTakePhoto" />

        <androidx.viewpager.widget.ViewPager
            android:id="@+id/viewPager"
            android:layout_width="0dp"
            android:layout_height="0dp"
            app:layout_constraintBottom_toTopOf="@id/backView"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>